import React, { FunctionComponent } from 'react';
import { View, Text } from 'react-native';

export const SwipeableItem: FunctionComponent = () => {
  return (
    <View>
      <Text>Test</Text>
    </View>
  );
};
