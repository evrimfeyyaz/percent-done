import React from 'react';
import { NavigationStackScreenComponent } from 'react-navigation-stack';
import { TimeMachine } from '../containers';

export const TimeMachineScreen: NavigationStackScreenComponent = () => {
  return <TimeMachine />;
};
